import React from 'react';
import { Link, Outlet } from 'react-router-dom';

const RelaxView = () => {
    return (
        <div>
            <h3 id="wip">Relax well!</h3>
        </div>
    )   
};

export default RelaxView;